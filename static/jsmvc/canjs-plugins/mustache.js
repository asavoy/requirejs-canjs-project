define([
    'can/view/mustache',
    // Components that use can.view.attr() must be registered before
    // templates are parsed.
    'components/can_view_attr/view_attr_example',
    // Mustache helpers must be registered before templates are rendered.
    'components/mustache_helper/mustache_helper_example'
], function() {

});
